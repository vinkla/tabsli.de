import App from "./App.svelte";

// TODO: Add first letter of text as favicon.
// TODO: Adjust font size based on viewport width.
// TODO: Add multiline support with text alignment support.

const app = new App({
  target: document.body,
});

export default app;
